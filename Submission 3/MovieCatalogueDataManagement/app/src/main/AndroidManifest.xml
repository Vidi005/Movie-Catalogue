<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dicoding.academies.moviecataloguedatamanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".data.source.local.room.dbinspector.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_tmdb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_tmdb"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCatalogueDataManagement"
        tools:ignore="AllowBackup">
        <activity android:name=".ui.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>